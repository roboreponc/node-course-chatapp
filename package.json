{
  "name": "09-chatapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && node node_modules/mocha/bin/mocha server/**/*.test.js --timeout 5000",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "8.11.1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "moment": "^2.22.2",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "expect": "^23.1.0",
    "mocha": "^5.2.0"
  }
}
